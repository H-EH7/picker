<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="picker.picker_backend.post.mapper.PostMapper">
    <select id="getPostLists" parameterType="string" resultType="picker.picker_backend.post.model.entity.PostEntity">
        SELECT * from picker_db.post
        WHERE is_deleted = 0;
    </select>

    <select id="getPost" parameterType="Long" resultType="picker.picker_backend.post.model.entity.PostEntity">
        SELECT * from picker_db.post
        WHERE is_deleted = 0
        AND post_id = #{postId};
    </select>

    <select id="getPostById" parameterType="string" resultType="picker.picker_backend.post.model.entity.PostEntity">
        SELECT * from picker_db.post
        where user_id = #{userId}
        and is_deleted = 0;
    </select>

    <insert id="insertPost" parameterType="picker.picker_backend.post.model.entity.PostEntity" useGeneratedKeys="true" keyProperty="postId" keyColumn="post_id">
        INSERT INTO picker_db.post
        (user_id, post_text, temp_id)
        VALUES(#{userId}, #{postText}, #{tempId});
    </insert>

    <update id="updatePost" parameterType="picker.picker_backend.post.model.entity.PostEntity">
        UPDATE picker_db.post
        SET post_text=#{postText}
        WHERE post_id=#{postId}
        and user_id=#{userId}
        and is_deleted = 0;
    </update>

    <delete id="deletePost" parameterType="picker.picker_backend.post.model.entity.PostEntity">
        UPDATE picker_db.post
        SET is_deleted=1
        WHERE post_id=#{postId}
        and user_id=#{userId}
    </delete>

    <update id="updateViewCountBatch" parameterType="picker.picker_backend.post.model.entity.PostEntity">
            UPDATE picker_db.post
            SET view_count = view_count + #{item.viewCount}
            WHERE post_id = #{item.postId}
    </update>
</mapper>
